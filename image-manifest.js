// Replaced at build time with real dimensions, tiny previews and content-hashed AVIF/WebP URLs.
// Keeping a fallback lets the existing, build-free GitHub Pages publication continue to work.
export const IMAGE_MANIFEST = {};
